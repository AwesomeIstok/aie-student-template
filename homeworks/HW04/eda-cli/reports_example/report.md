# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

### Настройки отчёта

- Заголовок отчёта: `EDA-отчёт`
- Макс. число гистограмм числовых признаков: `6`

## Качество данных (эвристики)

- Оценка качества: **0.74**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Есть ли колонки, где все значения одинаковые: **False**
- Проверка, что идентификатор (например, user_id) уникален: **True**

- Флаг, сигнализирующий, что есть категориальные признаки с очень большим числом уникальных значений (0.9): **False**

- Категориальные признаки: **[]**
## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Количество выводимых top_categories: 5

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

Построено не более **6** гистограмм для числовых признаков.
См. файлы `hist_*.png`.
